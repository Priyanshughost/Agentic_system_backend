
export const TOOL_REGISTRY = [
    {
        name: "web_search_tool",
        description: "Executes live web searches to find current information, news, or documentation."
    },
];

// Extract just the exact names for Zod to use as strict enums
export const AVAILABLE_TOOL_NAMES = TOOL_REGISTRY.map(tool => tool.name);